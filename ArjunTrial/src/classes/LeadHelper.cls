public class LeadHelper {
    
    public list<case> caselist = new list<case>();
    public string repMail='arjunanilkumar1995@gmail.com';
    public id lid;
    public string tomail;
    public void sendmail(list <case> c)
    {
        system.debug('trigger.new'+c);
        Messaging.SingleEmailMessage email = new Messaging.SingleEmailMessage();
        for(case clist:c)
        {
            system.debug('to'+clist.Lead__r.Email);
            if(clist.Lead__c !=null)
            {     
                lid=clist.Lead__c;
            }     
        }
        if(lid!=NULL)
        {
            list<lead> l1 = [select Email from Lead where id=:lid ];
            System.debug('lead'+l1);
            for(lead ll:l1)
            {
                string[] to = new string[]{ll.Email} ;
                    system.debug('toaddd'+to);
                email.setToAddresses(to);
            }
            
            email.setInReplyTo(repMail);
            email.setSubject('Test Mail');   
            email.setHtmlBody('Hello, <br/><br/>This is the test mail that you generated. <br/>The Email Id for which this mail was generated by <br/><br/>Regards<br/> Developer');
            Messaging.sendEmail(new Messaging.SingleEmailMessage[] { email });
            repMail = '';
        }
    }
}